apiVersion: v1
kind: Secret

metadata:
  name: backend-secret
  namespace: letstype

type: Opaque

# Replace these values with secrets managed by your GitOps or secrets tooling.
stringData:
  DB_PASSWORD: REPLACE_WITH_DB_PASSWORD
  JWT_SECRET: REPLACE_WITH_JWT_SECRET